import { ResetPasswordForm } from "@/features/auth";

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}

export const metadata = {
  title: "Reset Password — GTS's Finest",
};